"""Phase 3 production ML pipeline: dataset versioning, feature store, model training/
registry, and the autonomous improvement loop, built on top of FinSight's existing
price-ingestion and feature-computation code rather than duplicating it."""
